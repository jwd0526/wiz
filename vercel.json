{
    "version": 2,
    "buildCommand": "cd frontend && npm ci && npm run build",
    "outputDirectory": "frontend/dist",
    "routes": [
        {
            "src": "/wiz$",
            "dest": "/index.html"
        },
        {
            "src": "/wiz/(.*)",
            "dest": "/$1"
        }
    ]
}